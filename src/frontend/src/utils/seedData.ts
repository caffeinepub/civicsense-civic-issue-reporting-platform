/**
 * seedData.ts — Legacy file retained for compatibility.
 * All seed data is now managed directly by localStore.ts.
 * This file is intentionally empty.
 */
export function seedSampleData(): Promise<void> {
  return Promise.resolve();
}
